```
sudo apt-get install -y automake bc build-essential cpio curl file fzf git libncurses-devel libtool lzop make rsync unzip wget libssl-devel	python3 python3-module-pip autoconf libtool glib2-devel	
python3 -m pip install --user --break-system-packages kconfiglib
```
